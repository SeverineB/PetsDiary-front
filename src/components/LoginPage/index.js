import React from 'react';

import Header from './Header';
import Buttons from './Buttons';

const LoginPage = () => (
  <>
    <Header />
    <Buttons />
  </>
);

LoginPage.propTypes = {
};

export default LoginPage;
