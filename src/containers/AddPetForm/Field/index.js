import { connect } from 'react-redux';

import Field from '../../../components/Home/AddPetForm/Field';

const mapStateToProps = (state) => ({
});
const mapDispatchToProps = (dispatch) => ({
});

export default connect(
  mapStateToProps,
  mapDispatchToProps,
)(Field);
